<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Header Section -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp"
            android:elevation="2dp"
            android:background="@drawable/field_box_background"
            android:layout_marginBottom="12dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Event Administration"
                android:textSize="24sp"
                android:textStyle="bold"
                android:textColor="?android:attr/colorForeground"/>

            <TextView
                android:id="@+id/header_description_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Browse and administrate events."
                android:textSize="14sp"
                android:textColor="?android:attr/colorForeground"
                android:layout_marginTop="4dp"/>
        </LinearLayout>

        <!-- Upcoming Events Section -->
        <com.example.syzygy_eventapp.EventSummaryListView
            android:id="@+id/upcoming_event_list"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="24dp"/>

        <!-- Divider -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginVertical="8dp"
            android:background="?attr/colorOutline" />

        <!-- Past Events Section -->
        <com.example.syzygy_eventapp.EventSummaryListView
            android:id="@+id/past_event_list"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>


    </LinearLayout>
</LinearLayout>